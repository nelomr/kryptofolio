# Vault & SQLite Configuration
# DB_PATH is relative to process.cwd() or absolute. 
# If not set, it defaults to kryptofolio.db in the current working directory.
DB_PATH=../../kryptofolio.db

# KRYPTO_MASTER_KEY=your_base64_master_key_here # Fallback if Keychain is unavailable

# MOCK_MODE=true # Uncomment to use memory DB
