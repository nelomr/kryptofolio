# VITE_API_URL — The URL of the Kryptofolio backend (apps/backend).
# In development, apps/backend runs on port 3001 by default.
# Change this to point to your own backend (BYOB) if needed.
VITE_API_URL=http://localhost:3001

# Default Language
VITE_APP_LANG=en
